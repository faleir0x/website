---
// Fallback for any unknown URL. Without a top-level 404.html, Cloudflare Pages
// would treat the site as a SPA and answer every unknown path with a 200.
import NotFoundView from '../components/NotFoundView.astro';
---

<NotFoundView lang="en" />
