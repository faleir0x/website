---
// Emitted as /pt/404.html (see astro.config.mjs); Cloudflare Pages serves the
// nearest 404.html, so unknown /pt/… URLs get this one.
import NotFoundView from '../../components/NotFoundView.astro';
---

<NotFoundView lang="pt" />
